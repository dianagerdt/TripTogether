# TripTogether — пример переменных окружения
# Скопируйте в .env (cp env.example .env) и заполните значения.
# Файл .env в .gitignore — в репозиторий не попадает.

# --- База данных (для docker-compose) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=triptogether
DATABASE_URL=postgresql://postgres:postgres@db:5432/triptogether

# --- JWT (обязательно сменить в production) ---
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- DeepSeek API (нужен для генерации маршрутов) ---
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# --- Яндекс.Карты (опционально) ---
YANDEX_API_KEY=

# --- Приложение ---
APP_ENV=development
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000
MAX_GENERATION_COUNT=10
