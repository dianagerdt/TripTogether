# TripTogether MVP 🗺️

**Сервис для группового планирования путешествий с друзьями**

Помогает группе друзей собрать пожелания по местам, сгенерировать AI-маршруты, выбрать лучший голосованием и собрать чек-лист «Что взять» по выбранному маршруту.

## 🚀 Быстрый старт

### Требования
- Docker & Docker Compose
- DeepSeek API Key (для генерации маршрутов) - можно использовать OpenAI-совместимый API

### Запуск

1. **Клонировать репозиторий**
```bash
git clone <repo-url>
cd TripTogether
```

2. **Создать .env файл (опционально)**
```bash
# Скопировать пример и заполнить секреты (API ключи в .env не коммитятся)
cp env.example .env
# Заполнить DEEPSEEK_API_KEY и при необходимости JWT_SECRET
```

3. **Запустить**
```bash
docker-compose up -d
```

4. **Готово!**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

## 📋 Функциональность

### Аутентификация
- ✅ Регистрация и вход по email
- ✅ JWT токены (access + refresh)

### Поездки
- ✅ Создание поездки с датами
- ✅ Приглашение друзей по коду
- ✅ Просмотр участников

### Пожелания
- ✅ Добавление мест с приоритетом (1-5)
- ✅ Типы мест (музей, парк, еда, и т.д.)
- ✅ Комментарии к пожеланиям
- ✅ Просмотр пожеланий всех участников
- ✅ Редактирование пожеланий
- ✅ Фильтрация по типу и автору
- ✅ Сортировка по приоритету, дате, автору
- ✅ Автодополнение для стран и городов
- ✅ Сохранение последних введенных страны/города
- ✅ Реакции (эмодзи) на пожелания
- ✅ Группировка пожеланий по городам
- ✅ Визуализация пожеланий (Wish Cloud)

### AI Маршруты
- ✅ Генерация 3 вариантов маршрутов через DeepSeek API
- ✅ Лимит генераций (10 раз на поездку, настраивается)
- ✅ Объяснение выбора маршрута
- ✅ Markdown форматирование маршрутов
- ✅ Копирование маршрутов в буфер обмена
- ✅ Экспорт маршрутов в текстовый файл
- ✅ Прокручиваемые длинные маршруты

### Голосование
- ✅ Голосование за несколько вариантов
- ✅ Просмотр результатов в реальном времени
- ✅ Анимированные результаты голосования
- ✅ Визуализация победителя
- ✅ Прогресс-бары и проценты голосов

### Чек-лист «Что взять»
- ✅ Таб появляется после генерации маршрутов
- ✅ Генерация списка вещей по маршруту с большинством голосов (любой участник)
- ✅ Учёт стран/городов из пожеланий, дат и типа отдыха
- ✅ Категории: документы, одежда, аптечка, гаджеты и др.
- ✅ Кнопка генерации доступна только при наличии голосов за маршрут
- ✅ Общий чек-лист для всех участников поездки

## 🛠 Технологии

| Компонент | Технология |
|-----------|------------|
| Frontend | Next.js 14, TypeScript, Tailwind CSS, React Query, React Markdown |
| Backend | FastAPI, Python 3.11, SQLAlchemy, Pydantic |
| Database | PostgreSQL 15 |
| AI | DeepSeek API (OpenAI-совместимый) |
| Auth | JWT (access + refresh tokens) |
| Infrastructure | Docker, Docker Compose |
| UI Components | Custom Toast, Skeleton, EmptyState, Logo |

## 📁 Структура проекта

```
TripTogether/
├── docker-compose.yml
├── env.example
├── README.md
│
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── alembic/          # Миграции БД
│   └── app/
│       ├── main.py       # FastAPI app
│       ├── config.py     # Настройки
│       ├── database.py   # SQLAlchemy
│       ├── models/       # ORM модели
│       ├── schemas/      # Pydantic схемы
│       ├── routers/      # API эндпоинты
│       ├── services/     # Бизнес-логика (LLM)
│       ├── prompts/      # Системные промпты
│       └── utils/        # Хелперы (JWT, deps)
│
└── frontend/
    ├── Dockerfile
    ├── package.json
    └── src/
        ├── app/          # Next.js pages
        ├── components/   # React компоненты
        ├── contexts/     # AuthContext
        ├── lib/          # API клиенты
        └── types/        # TypeScript типы
```

## 🔧 Разработка

### Backend
```bash
# Зайти в контейнер
docker exec -it triptogether-backend bash

# Создать миграцию
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head
```

### Frontend
```bash
# Логи
docker logs -f triptogether-frontend
```

### Полезные команды
```bash
# Перезапуск сервисов
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Полная очистка
docker-compose down -v
```

## 📝 API Эндпоинты

### Auth
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/refresh` - Обновление токенов
- `GET /api/auth/me` - Текущий пользователь

### Trips
- `GET /api/trips` - Список поездок
- `POST /api/trips` - Создать поездку
- `GET /api/trips/{id}` - Детали поездки
- `POST /api/trips/join` - Присоединиться по коду
- `POST /api/trips/{id}/leave` - Покинуть поездку

### Preferences
- `GET /api/trips/{id}/preferences` - Список пожеланий
- `POST /api/trips/{id}/preferences` - Добавить пожелание
- `PUT /api/trips/{id}/preferences/{pref_id}` - Редактировать пожелание
- `DELETE /api/trips/{id}/preferences/{pref_id}` - Удалить
- `GET /api/trips/{id}/reactions` - Реакции на пожелания
- `POST /api/trips/{id}/preferences/{pref_id}/reactions` - Добавить реакцию
- `DELETE /api/trips/{id}/preferences/{pref_id}/reactions` - Удалить реакцию

### Routes
- `GET /api/trips/{id}/routes` - Список маршрутов
- `POST /api/trips/{id}/generate-routes` - Генерация AI

### Voting
- `POST /api/trips/{id}/votes` - Проголосовать
- `DELETE /api/trips/{id}/votes/{route_id}` - Отменить голос
- `GET /api/trips/{id}/my-votes` - Мои голоса
- `GET /api/trips/{id}/voting-results` - Результаты

### Checklist (Что взять)
- `GET /api/trips/{id}/checklist` - Получить чек-лист (или `null`, если ещё не сгенерирован)
- `POST /api/trips/{id}/generate-checklist` - Сгенерировать чек-лист по маршруту-победителю

## ⚙️ Конфигурация (.env)

Скопируйте `env.example` в `.env` и заполните значения. **Файл `.env` в .gitignore — в GitHub не попадает.**

```bash
cp env.example .env
```

Основные переменные см. в `env.example`. В production обязательно задайте свой `JWT_SECRET` (без этого бэкенд не запустится).

## 🔒 Безопасность: API ключи не в репозитории

- Файлы `.env`, `.env.local`, `.env.*.local` добавлены в `.gitignore` — не коммитятся.
- Секреты и API ключи (DEEPSEEK_API_KEY, YANDEX_API_KEY, JWT_SECRET) храните только в `.env` на своей машине или в секретах CI/CD.
- В репозитории только `env.example` с пустыми/примерными значениями — без реальных ключей.
- Если вы случайно закоммитили `.env` в прошлом, см. [GitHub: удаление чувствительных данных](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository).

## 🎨 UI/UX Особенности

- 🎯 Контекстные подсказки "Что дальше?"
- 📊 Визуализация прогресса планирования
- 🎨 Анимации и микроинтерактивности
- 📱 Адаптивный дизайн
- 🎭 Скелетоны загрузки
- 🎪 Toast-уведомления
- 🗺️ Логотип и брендинг

## 📚 Дополнительная документация

- `FEATURE_ROADMAP.md` - Roadmap с game-changing фичами
- `AI_MATCHMAKER_SPEC.md` - Детальная спецификация AI Matchmaker
- `TESTING_REPORT.md` - Отчет о тестировании

## 🚀 Недавние обновления

- ✅ **Чек-лист «Что взять»** — AI-список вещей по выбранному маршруту, таб после голосования
- ✅ Переход на DeepSeek API
- ✅ Увеличен лимит генераций до 10
- ✅ Добавлен логотип и favicon
- ✅ Markdown рендеринг маршрутов
- ✅ Редактирование пожеланий
- ✅ Фильтрация и сортировка
- ✅ Автодополнение городов/стран
- ✅ Сохранение последних введенных данных
- ✅ Реакции на пожелания
- ✅ Экспорт маршрутов

## 📄 Лицензия

MIT

---

Made with ❤️ for travelers
