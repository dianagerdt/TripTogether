# 🎲 AI Matchmaker - Детальная спецификация

## 🎯 Концепция

**AI Matchmaker** - это умный аналитик, который изучает пожелания всех участников группы и находит скрытые паттерны, пересечения интересов и возможности для улучшения маршрута.

---

## 🧠 Как это работает

### Этап 1: Анализ пожеланий

Когда участники добавляют пожелания, AI Matchmaker анализирует:

#### 1.1 Семантический анализ
```
Входные данные:
- Анна: "Лувр, Париж, музей, приоритет 5, комментарий: 'Хочу увидеть Мону Лизу'"
- Петя: "Орсе, Париж, музей, приоритет 4, комментарий: 'Интересуюсь импрессионистами'"
- Маша: "Эйфелева башня, Париж, viewpoint, приоритет 5"

AI анализирует:
- Типы мест: 2 музея, 1 смотровая площадка
- Тематика: искусство (Лувр, Орсе), архитектура (Эйфелева башня)
- Приоритеты: высокие (4-5)
- География: все в Париже
```

#### 1.2 Поиск паттернов
- **Тематические кластеры**: "Все любят искусство" → предлагает еще музеи
- **Географические кластеры**: "Все в одном районе" → оптимизирует маршрут
- **Временные паттерны**: "Много музеев" → предлагает разбавить активностями
- **Приоритетные пересечения**: "У всех приоритет 5" → обязательно включить

### Этап 2: Генерация инсайтов

AI генерирует умные выводы:

#### Примеры инсайтов:

**Инсайт 1: Тематическое пересечение**
```
🎨 "Отличная новость! Вы все любите искусство:
   - Анна хочет увидеть классику (Лувр)
   - Петя интересуется импрессионистами (Орсе)
   - Оба музея в Париже, можно посетить за один день!
   
   💡 Рекомендация: Добавьте музей Родена - он между ними 
      и идеально дополнит вашу художественную программу."
```

**Инсайт 2: Географическая оптимизация**
```
📍 "Ваши пожелания очень близко друг к другу:
   - Все места в радиусе 2 км
   - Можно пройти пешком за один день
   
   💡 Рекомендация: Добавьте места в других районах Парижа,
      чтобы увидеть больше города."
```

**Инсайт 3: Баланс интересов**
```
⚖️ "Анализ интересов группы:
   - Анна: искусство (40%), история (30%), еда (30%)
   - Петя: искусство (50%), архитектура (30%), развлечения (20%)
   - Маша: архитектура (60%), фото (40%)
   
   💡 Рекомендация: Добавьте больше мест для фото и развлечений,
      чтобы сбалансировать интересы Маши."
```

**Инсайт 4: Скрытые возможности**
```
🔍 "Мы заметили интересный паттерн:
   - Вы все выбрали места в центре Парижа
   - Но никто не упомянул Монмартр
   
   💡 Рекомендация: Монмартр идеально подходит для вашей группы:
      искусство (Музей Сальвадора Дали), архитектура (Сакре-Кёр),
      и отличные фото-точки!"
```

### Этап 3: Визуализация совместимости

#### 3.1 Матрица совместимости
```
        Анна  Петя  Маша
Анна    100%  85%   60%
Петя     85%  100%   70%
Маша     60%   70%  100%

Общая совместимость группы: 75% ⭐⭐⭐⭐
```

#### 3.2 Граф интересов
```
[Визуализация]
Анна ──[искусство]── Петя
  │                    │
[история]          [архитектура]
  │                    │
  └────[Париж]─────────┘
           │
        Маша
```

#### 3.3 Heatmap интересов
```
Тип места:    Анна  Петя  Маша  Группа
Музеи         ████  ████  ██    ████████
Архитектура   ██    ███   █████ ███████
Еда           ███   ██    ██    ████
Активности    █     ███   ███   ████
```

---

## 🎨 UX/UI Реализация

### Вариант 1: Виджет на странице пожеланий

```
┌─────────────────────────────────────────┐
│  🎲 AI Matchmaker - Анализ группы      │
├─────────────────────────────────────────┤
│                                          │
│  📊 Совместимость группы: 85% ⭐⭐⭐⭐   │
│                                          │
│  🎯 Ключевые инсайты:                   │
│                                          │
│  ✅ Вы все любите искусство!            │
│     → Рекомендуем добавить музей Родена │
│                                          │
│  ⚠️  Недостаточно активностей          │
│     → Добавьте парк или развлечения     │
│                                          │
│  💡 Скрытая возможность:                │
│     Монмартр идеально подходит группе   │
│                                          │
│  [Показать все рекомендации]            │
└─────────────────────────────────────────┘
```

### Вариант 2: Отдельная вкладка "Анализ"

```
[Пожелания] [Маршруты] [Голосование] [Анализ] ← новая вкладка

┌─────────────────────────────────────────┐
│  🎲 AI Matchmaker                        │
├─────────────────────────────────────────┤
│                                          │
│  📈 Общая статистика                    │
│  ┌─────────┬─────────┬─────────┐        │
│  │ Участники│Пожелания│Совместимость│   │
│  │    3     │   12    │   85%   │        │
│  └─────────┴─────────┴─────────┘        │
│                                          │
│  🎯 Топ-3 инсайта:                      │
│  1. Все любят искусство                  │
│  2. Географически близко                 │
│  3. Нужно больше активностей             │
│                                          │
│  💡 Умные рекомендации:                 │
│  • Музей Родена (искусство)             │
│  • Парк Тюильри (активности)             │
│  • Монмартр (фото + искусство)           │
│                                          │
│  📊 Детальный анализ:                   │
│  [Показать матрицу совместимости]       │
│  [Показать граф интересов]               │
│  [Показать heatmap]                      │
└─────────────────────────────────────────┘
```

### Вариант 3: Интеграция в процесс добавления пожеланий

Когда пользователь добавляет пожелание:

```
[Добавление пожелания]

🎲 AI Matchmaker заметил:
"Это место отлично сочетается с пожеланием Пети!
Оба музея можно посетить за один день."

[Показать на карте, как они связаны]
```

---

## 🔧 Техническая реализация

### Backend: Аналитический сервис

```python
# backend/app/services/matchmaker_service.py

from typing import List, Dict
from app.models import PlacePreference, Trip
from collections import Counter, defaultdict
import re

class MatchmakerService:
    def __init__(self):
        self.art_keywords = ['музей', 'галерея', 'искусство', 'картина', 
                            'скульптура', 'импрессионист', 'классика']
        self.activity_keywords = ['парк', 'активность', 'развлечение', 
                                  'аттракцион', 'квест']
        # ... другие категории
    
    def analyze_group(self, preferences: List[PlacePreference]) -> Dict:
        """Основной метод анализа группы"""
        
        # 1. Семантический анализ
        themes = self._extract_themes(preferences)
        
        # 2. Географический анализ
        geography = self._analyze_geography(preferences)
        
        # 3. Анализ приоритетов
        priorities = self._analyze_priorities(preferences)
        
        # 4. Поиск паттернов
        patterns = self._find_patterns(preferences, themes, geography)
        
        # 5. Генерация инсайтов
        insights = self._generate_insights(patterns, themes, geography)
        
        # 6. Расчет совместимости
        compatibility = self._calculate_compatibility(preferences)
        
        # 7. Генерация рекомендаций
        recommendations = self._generate_recommendations(
            preferences, themes, geography, patterns
        )
        
        return {
            'compatibility_score': compatibility,
            'insights': insights,
            'recommendations': recommendations,
            'patterns': patterns,
            'themes': themes,
            'geography': geography
        }
    
    def _extract_themes(self, preferences: List[PlacePreference]) -> Dict:
        """Извлечение тематик из пожеланий"""
        themes = defaultdict(list)
        
        for pref in preferences:
            # Анализ типа места
            if pref.place_type == 'museum':
                themes['art'].append(pref)
            elif pref.place_type == 'park':
                themes['nature'].append(pref)
            # ... другие типы
            
            # Анализ комментария (NLP)
            if pref.comment:
                comment_lower = pref.comment.lower()
                if any(kw in comment_lower for kw in self.art_keywords):
                    themes['art'].append(pref)
                # ... другие категории
        
        return dict(themes)
    
    def _analyze_geography(self, preferences: List[PlacePreference]) -> Dict:
        """Географический анализ"""
        cities = Counter([p.city for p in preferences])
        countries = Counter([p.country for p in preferences])
        
        # Группировка по городам
        city_groups = defaultdict(list)
        for pref in preferences:
            city_groups[pref.city].append(pref)
        
        return {
            'cities': dict(cities),
            'countries': dict(countries),
            'city_groups': dict(city_groups),
            'is_concentrated': len(cities) <= 2,  # Все в 1-2 городах
            'needs_diversification': len(cities) == 1  # Все в одном городе
        }
    
    def _find_patterns(self, preferences, themes, geography) -> List[Dict]:
        """Поиск интересных паттернов"""
        patterns = []
        
        # Паттерн 1: Тематическое единство
        if len(themes) == 1:
            patterns.append({
                'type': 'thematic_unity',
                'description': f'Все участники интересуются {list(themes.keys())[0]}',
                'strength': 'high'
            })
        
        # Паттерн 2: Географическая концентрация
        if geography['is_concentrated']:
            patterns.append({
                'type': 'geographic_concentration',
                'description': 'Все места в одном/двух городах',
                'strength': 'medium'
            })
        
        # Паттерн 3: Высокие приоритеты
        high_priority_count = sum(1 for p in preferences if p.priority >= 4)
        if high_priority_count / len(preferences) > 0.7:
            patterns.append({
                'type': 'high_priority',
                'description': 'Большинство пожеланий с высоким приоритетом',
                'strength': 'high'
            })
        
        return patterns
    
    def _generate_insights(self, patterns, themes, geography) -> List[Dict]:
        """Генерация инсайтов на основе паттернов"""
        insights = []
        
        # Инсайт 1: Тематическое пересечение
        if 'thematic_unity' in [p['type'] for p in patterns]:
            main_theme = list(themes.keys())[0]
            insights.append({
                'type': 'thematic_match',
                'title': f'Вы все любите {main_theme}!',
                'description': f'Все участники выбрали места, связанные с {main_theme}',
                'recommendation': f'Добавьте еще места в категории {main_theme}',
                'priority': 'high'
            })
        
        # Инсайт 2: Географическая оптимизация
        if geography['needs_diversification']:
            insights.append({
                'type': 'geographic_diversification',
                'title': 'Все места в одном городе',
                'description': 'Ваши пожелания сосредоточены в одном месте',
                'recommendation': 'Добавьте места в других городах/районах',
                'priority': 'medium'
            })
        
        # Инсайт 3: Баланс интересов
        theme_distribution = {k: len(v) for k, v in themes.items()}
        if len(theme_distribution) > 1:
            dominant_theme = max(theme_distribution, key=theme_distribution.get)
            insights.append({
                'type': 'balance_needed',
                'title': f'Доминирует тема: {dominant_theme}',
                'description': f'Большинство мест связаны с {dominant_theme}',
                'recommendation': 'Добавьте разнообразия в другие категории',
                'priority': 'low'
            })
        
        return insights
    
    def _calculate_compatibility(self, preferences: List[PlacePreference]) -> float:
        """Расчет совместимости группы (0-100%)"""
        if len(preferences) < 2:
            return 100.0
        
        # Анализ пересечений интересов
        user_themes = defaultdict(set)
        for pref in preferences:
            theme = self._get_theme(pref)
            user_themes[pref.user_id].add(theme)
        
        # Расчет пересечений между парами
        users = list(user_themes.keys())
        intersections = []
        for i in range(len(users)):
            for j in range(i+1, len(users)):
                common = len(user_themes[users[i]] & user_themes[users[j]])
                total = len(user_themes[users[i]] | user_themes[users[j]])
                if total > 0:
                    intersections.append(common / total)
        
        # Средняя совместимость
        compatibility = sum(intersections) / len(intersections) * 100 if intersections else 0
        return round(compatibility, 1)
    
    def _generate_recommendations(self, preferences, themes, geography, patterns) -> List[Dict]:
        """Генерация умных рекомендаций"""
        recommendations = []
        
        # Рекомендация 1: Дополнить доминирующую тему
        if themes:
            dominant_theme = max(themes.items(), key=lambda x: len(x[1]))
            recommendations.append({
                'type': 'complement_theme',
                'theme': dominant_theme[0],
                'suggestion': f'Добавьте еще места в категории {dominant_theme[0]}',
                'reason': f'Группа активно интересуется {dominant_theme[0]}'
            })
        
        # Рекомендация 2: Географические дополнения
        if geography['needs_diversification']:
            main_city = list(geography['city_groups'].keys())[0]
            recommendations.append({
                'type': 'geographic_expansion',
                'suggestion': f'Добавьте места в других районах {main_city} или соседних городах',
                'reason': 'Все текущие места в одном районе'
            })
        
        # Рекомендация 3: Баланс типов мест
        place_types = Counter([p.place_type for p in preferences])
        if len(place_types) == 1:
            recommendations.append({
                'type': 'diversify_types',
                'suggestion': f'Добавьте места других типов (не только {list(place_types.keys())[0]})',
                'reason': 'Разнообразие сделает поездку интереснее'
            })
        
        return recommendations
    
    def _get_theme(self, preference: PlacePreference) -> str:
        """Определение темы пожелания"""
        if preference.place_type == 'museum':
            return 'art'
        elif preference.place_type == 'park':
            return 'nature'
        elif preference.place_type == 'food':
            return 'food'
        # ... другие типы
        return 'other'
```

### API Endpoint

```python
# backend/app/routers/matchmaker.py

@router.get("/{trip_id}/matchmaker", response_model=MatchmakerResponse)
def get_matchmaker_analysis(
    trip_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    """Получить анализ группы от AI Matchmaker"""
    trip = get_trip_or_404(trip_id, db)
    check_user_is_participant(trip_id, current_user.id, db)
    
    preferences = db.query(PlacePreference).filter(
        PlacePreference.trip_id == trip_id
    ).all()
    
    if len(preferences) < 2:
        return MatchmakerResponse(
            compatibility_score=100.0,
            insights=[],
            recommendations=[],
            message="Добавьте больше пожеланий для анализа"
        )
    
    matchmaker = MatchmakerService()
    analysis = matchmaker.analyze_group(preferences)
    
    return MatchmakerResponse(**analysis)
```

### Frontend: Компонент

```typescript
// frontend/src/components/MatchmakerAnalysis.tsx

interface MatchmakerInsight {
  type: string
  title: string
  description: string
  recommendation: string
  priority: 'high' | 'medium' | 'low'
}

interface MatchmakerData {
  compatibility_score: number
  insights: MatchmakerInsight[]
  recommendations: Array<{
    type: string
    suggestion: string
    reason: string
  }>
}

export function MatchmakerAnalysis({ tripId }: { tripId: number }) {
  const { data, isLoading } = useQuery<MatchmakerData>({
    queryKey: ['matchmaker', tripId],
    queryFn: () => api.get(`/api/trips/${tripId}/matchmaker`).then(r => r.data),
    enabled: !!tripId
  })
  
  if (isLoading) return <Skeleton />
  if (!data) return null
  
  return (
    <div className="card">
      <div className="flex items-center gap-2 mb-4">
        <span className="text-2xl">🎲</span>
        <h3 className="text-lg font-semibold">AI Matchmaker</h3>
      </div>
      
      {/* Совместимость */}
      <div className="mb-6">
        <div className="flex items-center justify-between mb-2">
          <span className="text-sm text-gray-600">Совместимость группы</span>
          <span className="text-2xl font-bold text-primary-600">
            {data.compatibility_score}%
          </span>
        </div>
        <div className="w-full bg-gray-200 rounded-full h-3">
          <div 
            className="bg-primary-500 h-3 rounded-full transition-all"
            style={{ width: `${data.compatibility_score}%` }}
          />
        </div>
      </div>
      
      {/* Инсайты */}
      {data.insights.length > 0 && (
        <div className="mb-6">
          <h4 className="font-semibold mb-3">🎯 Ключевые инсайты</h4>
          <div className="space-y-3">
            {data.insights.map((insight, i) => (
              <div 
                key={i}
                className={`p-3 rounded-lg border-l-4 ${
                  insight.priority === 'high' ? 'bg-green-50 border-green-500' :
                  insight.priority === 'medium' ? 'bg-yellow-50 border-yellow-500' :
                  'bg-blue-50 border-blue-500'
                }`}
              >
                <div className="font-semibold mb-1">{insight.title}</div>
                <div className="text-sm text-gray-600 mb-2">
                  {insight.description}
                </div>
                <div className="text-sm font-medium text-primary-600">
                  💡 {insight.recommendation}
                </div>
              </div>
            ))}
          </div>
        </div>
      )}
      
      {/* Рекомендации */}
      {data.recommendations.length > 0 && (
        <div>
          <h4 className="font-semibold mb-3">💡 Умные рекомендации</h4>
          <ul className="space-y-2">
            {data.recommendations.map((rec, i) => (
              <li key={i} className="flex items-start gap-2">
                <span className="text-primary-500">•</span>
                <div>
                  <div className="font-medium">{rec.suggestion}</div>
                  <div className="text-xs text-gray-500">{rec.reason}</div>
                </div>
              </li>
            ))}
          </ul>
        </div>
      )}
    </div>
  )
}
```

---

## 🎯 Примеры работы

### Пример 1: Группа любителей искусства

**Входные данные:**
- Анна: Лувр (музей, приоритет 5)
- Петя: Орсе (музей, приоритет 4)
- Маша: Эйфелева башня (viewpoint, приоритет 5)

**AI Matchmaker анализирует:**
```
Тематический анализ:
- 2 музея (искусство)
- 1 смотровая площадка (архитектура)
- Все в Париже

Инсайты:
1. ✅ "Анна и Петя оба любят искусство - отличное совпадение!"
2. ⚠️  "Маша больше интересуется архитектурой - добавьте баланс"
3. 💡 "Все места близко - можно оптимизировать маршрут"

Рекомендации:
• Музей Родена (искусство + архитектура - идеально для всех)
• Монмартр (архитектура для Маши + искусство для других)
• Парк Тюильри (разбавить музеи активностью)

Совместимость: 75%
```

### Пример 2: Разношерстная группа

**Входные данные:**
- Анна: Лувр (музей, приоритет 5)
- Петя: Диснейленд (активность, приоритет 5)
- Маша: Версаль (музей, приоритет 4)

**AI Matchmaker анализирует:**
```
Тематический анализ:
- 2 музея (искусство/история)
- 1 активность (развлечения)
- Разные локации (Париж, Париж, Версаль)

Инсайты:
1. ⚠️  "Интересы группы различаются - нужен баланс"
2. 💡 "Петя хочет развлечений - добавьте больше активностей"
3. ✅ "Анна и Маша оба любят историю - можно объединить"

Рекомендации:
• Музей Орсе (для Анны и Маши)
• Парк развлечений (для Пети)
• Компромиссное место: Монмартр (искусство + развлечения)

Совместимость: 60%
```

---

## 🚀 План реализации

### Фаза 1: Базовая версия (1-2 недели)
- [ ] Семантический анализ пожеланий
- [ ] Расчет совместимости
- [ ] Базовые инсайты (тематические, географические)
- [ ] Простые рекомендации

### Фаза 2: Расширенная версия (2-3 недели)
- [ ] Интеграция с LLM для более умных рекомендаций
- [ ] Визуализация (графы, heatmap)
- [ ] Детальная матрица совместимости
- [ ] Интеграция с картами для географических рекомендаций

### Фаза 3: Продвинутая версия (3-4 недели)
- [ ] Машинное обучение на основе прошлых поездок
- [ ] Персонализированные рекомендации
- [ ] Предсказание успешности маршрута
- [ ] Интеграция с внешними API (TripAdvisor, Google Places)

---

## 💡 Дополнительные идеи

1. **"Совместимость с прошлыми поездками"**: "Группы с похожими интересами выбирали эти места"
2. **"Конфликт-детектор"**: "Анна и Петя хотят разные вещи - вот компромисс"
3. **"Тайм-машина"**: "Если бы вы поехали в другое время года, вот что бы изменилось"
4. **"Что если добавить..."**: Интерактивный симулятор добавления новых мест

---

**Готовы начать реализацию? Это будет действительно крутая фича! 🚀**
